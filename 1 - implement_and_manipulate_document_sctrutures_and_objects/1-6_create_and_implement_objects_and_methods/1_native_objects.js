// Same native objects are available statically
var squareValue = Math.sqrt(144);

// Outros objetos requer que sejam criados como abaixo:
var listOfPrimeNumbers = new Array(1, 2, 3, 5, 7, 11, 13, 17, 19, 23);

// wrapper objects